<h4>Search for a book by title, author, or ISBN</h4>

<%= bootstrap_form_tag url: books_search_path, method: :get do |f| %>
  <%= f.text_field :criteria %>
  <button class="btn btn-primary btn-sm">Submit</button>
<% end %>

<% if @books %>
  <table class="table table-hover table-striped table-condensed">
    <thead>
      <tr>
        <th>Title</th>
        <th>Author</th>
        <th>ISBN</th>
        <th>Subject Area</th>
        <th>Status</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @books.each do |b| %>
      </tr>
        <th><%= b.title %></th>
        <th><%= b.author %></th>
        <th><%= b.isbn %></th>
        <th><%= b.category %></th>
        <th><%= b.status %></th>
        <th class="button-container">
          <%= button_to('Go To Book', book_path(b), class: 'btn btn-xs btn-primary', method: :get) %>
        </th>
      </tr>
      <% end %>
    </tbody>
  </table>
<% end %>
