<h2>
  Current Purchase Requests for <%= @campus.city %>
  <span class="badge"><%= @prs.count %></span>
</h2>

<table class="table table-hover table-striped table-condensed">
  <thead>
    <tr>
      <th>Book Title</th>
      <th>Book Author</th>
      <th>Book ISBN</th>
      <th>Subject Area</th>
      <th>Made By</th>
      <th>Status</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @prs.each do |p| %>
    </tr>
      <th><%= p.title %></th>
      <th><%= p.author %></th>
      <th><%= p.isbn %></th>
      <th><%= p.klass %></th>
      <th><%= p.user.email %></th>
      <th><%= p.status %></th>
      <th class="button-container">
        <%= button_to('Edit', edit_purchase_request_path(p), class: 'btn btn-xs btn-primary', method: :get) %>
      </th>
    </tr>
    <% end %>
  </tbody>
</table>
