<h2>
  Active Purchase Requests for <%= @campus.city %>
  <span class="badge"><%= @prs.count %></span>
</h2>

<table class="table table-hover table-striped table-condensed">
  <thead>
    <tr>
      <th>Book Title</th>
      <th>Book Author</th>
      <th>Book ISBN</th>
      <th>Subject Area</th>
      <th>Made By</th>
      <th>Status</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @prs.each do |p| %>
    </tr>
      <th><%= p.title %></th>
      <th><%= p.author %></th>
      <th><%= p.isbn %></th>
      <th><%= p.klass %></th>
      <th><%= p.user.email %></th>
      <th><%= p.status %></th>
      <th class="button-container">
        <%= button_to('Edit', edit_purchase_request_path(p), class: 'btn btn-xs btn-primary', method: :get) %>
      </th>
    </tr>
    <% end %>
  </tbody>
</table>

<h2>
  Inactive Purchase Requests for <%= @campus.city %>
  <span class="badge"><%= @prs_past.count %></span>
</h2>

<table class="table table-hover table-striped table-condensed">
  <thead>
    <tr>
      <th>Book Title</th>
      <th>Book ISBN</th>
      <th>Status</th>
    </tr>
  </thead>
  <tbody>
    <% @prs_past.each do |p| %>
    </tr>
      <th><%= p.title %></th>
      <th><%= p.isbn %></th>
      <th><%= p.status %></th>
    </tr>
    <% end %>
  </tbody>
</table>
